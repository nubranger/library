<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img src="{{ asset('build/images/book.png') }}" alt="" width="50" height="50"
                 class="d-inline-block align-text-top">
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('book_index') }}">Book list</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('book_create') }}">Add Book</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('author_index') }}">Author list</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('author_create') }}">Add Author</a>
                </li>
                {% endif %}

            </ul>
            <form class="d-flex">
                {# <button class="btn btn-outline-success" type="submit">Register</button> #}
                {% if app.user %}
                    <div class="input-group">
                        <div class="input-group-text" id="btnGroupAddon">{{ app.user.username }}</div>
                        <a href="{{ path('app_logout') }}" type="button" class="btn btn-outline-primary">Logout</a>
                    </div>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-primary me-2" type="button">Sign in</a>
                    <a href="{{ path('app_register') }}" class="btn btn-outline-primary me-2" type="button">Register</a>
                {% endif %}
            </form>
        </div>
    </div>
</nav>

